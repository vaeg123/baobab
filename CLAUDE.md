# BAOBAB

Legal Operating System for Africa — MVP CIMA.

## Stack
Python 3.11 / FastAPI / PostgreSQL / Neo4j / pgvector

## Commands
- `uvicorn baobab.api.main:app --reload` — start API
- `pytest` — run tests
- `docker compose up -d` — start databases

## Architecture
See docs/architecture.md
